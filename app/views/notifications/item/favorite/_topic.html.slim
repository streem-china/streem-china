.title
  - if notification.unread?
    span.readflag &bull;&nbsp;
  = sanitize(t('notification.favorite/topic_link',
                user: link_to(favorite.user.name, user_path(favorite.user.name), class: :light),
                topic: link_to(topic.title.truncate(30), topic_path(topic))),
             tags: %w(a))
.tips.lightsmall
  span =timeago_tag(notification.created_at)
