|
  center = '<center class="deleted"><p><i><s> #{t("floor_already_deleted", floor: @reply.floor)}</s></i></p></center>';
  $("#reply-#{@reply.floor}").html(center);
  Flash.notify('success', '#{t("replies.deleted_success")}');

